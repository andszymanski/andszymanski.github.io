import React from 'react';

const Heading = (props) =>{
    return <h1 className="main-heading">{props.headingText}</h1>;
    return <h2 className="next-heading">{props.nextHeadingText}</h2>;
}
export default Heading;


